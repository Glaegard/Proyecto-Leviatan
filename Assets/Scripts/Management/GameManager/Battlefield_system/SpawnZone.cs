using UnityEngine;

public class SpawnZone : MonoBehaviour
{
    [Tooltip("Índice del carril (0, 1, 2, ...). Asegúrate de que coincida con la configuración en el LaneManager.")]
    public int laneIndex;
}
